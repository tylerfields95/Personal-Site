
https://docs.nestjs.com/

Method names within a controller are completely arbitrary and the routing is handled through specified strings in the decorators!

you can use RXJS on the back end for async shit. how neat (though, i don't entirely know how that matters in the context of a controller, but still cool).

Services work pretty much exactly as I would expect. they get marked with the @Injectable decorator and are imported into a providers array within a module. 


tomorrow, i would like to finish the overview docs at minimum.

in total, i feel that i need to read the Overview, fundamentals, Techniques, Security docs, and maybe the FAQs before i can really move on. 

from what I have read so far, i think i basically understand what i generated with Claude before. i just have a simple module setup with a controller that spits out a static array of blog objects. 